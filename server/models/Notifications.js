import mongoose from "mongoose";

const notificationSchema = new mongoose.Schema({
    // if, userid, type, contact, message, timesent. There may be more collections and attributes we will add as we develop.
});